# 日檢弱點分析 — 自動評語產生網頁

## 任務

做一個**純前端、免後端**的網頁：使用者輸入一位（或一批）學生的 JLPT 正式成績，網頁自動判定並組出一段老師口吻的評語。部署目標是 GitHub Pages 或直接開啟 `index.html`，不需要伺服器、不需要登入。

使用者是台灣高中的日文老師（ヨッシー），介面用**繁體中文**。學生會自己來輸入成績看評語，老師也會用批次模式一次產生全班的評語貼回 Google Sheet。

## 這個 repo 已經有什麼（不要重做）

| 路徑 | 內容 | 狀態 |
|---|---|---|
| `data/levels.json` | 五個級數的及格分、各科滿分／門檻／分段上限、下一級 | ✅ 定案，直接用 |
| `data/comments.json` | 172 筆評語，key 格式 `級數\|表\|區間代碼\|參考情報` | ✅ 定案，直接用，**不要改寫任何評語文字** |
| `src/logic.js` | 判定 + 組裝的參考實作（`judge` / `assemble` / `validate`），純函式、零依賴、ESM | ✅ 已通過測試，直接 import |
| `tests/logic.test.js` + `tests/fixtures.json` | 6 個測試，含三位範例學生的完整預期輸出、所有 key 可達性 | ✅ `npm test` 全綠，任何改動後都要保持綠燈 |
| `reference/*.xlsx` | 老師手上的 Excel 版（同一份資料 + 公式試算），只供對照 | 唯讀 |
| `reference/comments.py`, `build.py` | 產生 xlsx 與 JSON 的來源，評語文字的唯一真相在這裡 | 唯讀；若老師之後改評語，改這裡再重新匯出 JSON |

`src/logic.js` 就是整個業務邏輯。你的工作是把它包成好用的網頁，不是重新發明判定規則。

## 業務規則摘要（細節看 `src/logic.js`，這裡只講你需要理解的）

1. **級數決定欄位**
   - N1／N2／N3：言語知識（0–60）、讀解（0–60）、聽解（0–60），三科
   - N4／N5：言語知識・讀解（0–120）、聽解（0–60），兩科
   - 每級都要輸入參考情報「文字・語彙」「文法」各一個 A／B／C
   - 總分 = 各科相加，由網頁自動算，不要讓使用者另外填
2. **合格判定**：總分 ≥ 及格分 **且** 每一科 ≥ 該科門檻。總分夠但有一科沒過 → 「單科未達」，這是老師最在意的情況，UI 要明確標出是哪一科沒過。
3. **評語組裝順序**：開頭句 → 言語知識評語 → 讀解評語（僅 N1–N3）→ 聽解評語 → 結尾句。輸出是一段連續文字，中間不換行、不加標題。
4. **〇〇 代入**：參考情報有一項是 C 時，評語裡的「〇〇」要代成「單字」或「文法」。`assemble()` 已處理。
5. **12 月建議**已包含在結尾句裡，不用另外做。

## 要做的功能

### 必做（MVP）
- **單人模式**：選級數 → 欄位依級數動態切換 → 輸入分數與參考情報 → 即時顯示：總分、合格／不合格（含差幾分、哪科沒過線）、完整評語、一鍵複製。
- **輸入驗證**：用 `validate()`，超出範圍或漏填要有中文提示，不要讓錯誤輸入產生評語。
- **批次模式**：貼上或上傳 CSV／從 Google Sheet 複製的 TSV，欄位順序：`姓名, 級數, 言語知識, 讀解, 聽解, 文字語彙, 文法`（N4/N5 的讀解欄留空）。逐列產生評語，顯示成表格，可**下載 CSV**（含姓名、級數、總分、判定、評語）方便貼回 Google Sheet。有錯的列要標紅並說明原因，不要整批失敗。
- **手機可用**：學生會用手機開。單欄排版，觸控友善。
- **評語資料外部化**：從 `data/comments.json` 與 `data/levels.json` 讀取，不要把文字寫死在元件裡。老師之後會改評語，改 JSON 就要生效。

### 加分（有餘力再做，做的話一次做完，不要做一半）
- 老師模式的「判定明細」摺疊區：顯示各科區間代碼、參考情報代碼、四個查表 key，方便老師 debug 為什麼出現某段評語。
- 全班統計：批次模式下顯示各級合格率、哪一科最多人沒過線、參考情報 C 集中在單字還是文法。
- 評語編輯器：在網頁上直接改 `comments.json` 的內容並匯出 JSON（不需要存後端）。

### 不要做
- 不要做後端、資料庫、登入。
- 不要用 localStorage 以外的任何持久化；MVP 連 localStorage 都不需要。
- 不要改評語文字、不要「潤飾」評語、不要改分段線。這些是老師定案的內容。
- 不要引入大型框架。優先順序：純 HTML + ES modules（單一 `index.html` + `src/`）> 輕量框架。如果要用打包工具，用 Vite，並確保 `npm run build` 產物能直接丟 GitHub Pages。

## 技術限制
- Node 22，ESM（`package.json` 已設 `"type": "module"`）。
- `src/logic.js` 用了 `String.prototype.replaceAll`，目標瀏覽器為近三年的 Chrome／Safari／Edge 即可。
- `data/*.json` 用 `fetch` 或 `import ... with { type: "json" }` 載入都可以，但要能在 `file://` 直接開啟時運作（fetch 在 file:// 下會失敗，若走純靜態方案請改為 `<script type="module">` 內 import，或提供一個 `data/index.js` 把 JSON 包成 ESM 匯出）。
- 中文字型不要另外載，用系統字型（`system-ui, "Microsoft JhengHei", "PingFang TC", sans-serif`）。

## 驗收
1. `npm test` 全綠。
2. 用 `tests/fixtures.json` 的三位學生在單人模式輸入，畫面上的評語與 fixture 的 `text` **逐字相同**。
3. N5 學生：言語知識・讀解 70、聽解 14、參考情報 B／A → 顯示「不合格・聽解未達門檻（差 5 分）」，評語開頭是「這次總分其實已經到了…」。
4. 批次模式貼入三列（含一列故意填 130 分的錯誤資料）→ 兩列正常產生、一列標紅，下載的 CSV 用 Excel 開啟中文不亂碼（加 BOM）。
5. 手機寬度（375px）下不需橫向捲動。

## 工作方式
- 先讀 `src/logic.js` 與 `tests/logic.test.js`，確定理解 key 的組成方式再動手。
- 一次做完 MVP 再回報，不要每做一個元件就停下來問。有疑問的地方自己下合理決定並在 README 記錄。
- 完成後更新 `README.md`：怎麼在本機開、怎麼部署到 GitHub Pages、老師怎麼改評語。
